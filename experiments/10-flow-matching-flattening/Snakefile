# 10-flow-matching-flattening
# Flow-matching flattening

rule all:
    input:
        "report.qmd",

rule render_report:
    input:
        "report.qmd",
    output:
        "report.html",
    shell:
        "pixi run quarto render {input}"
